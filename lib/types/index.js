/**
 * Chat history navigator plugin, node half.
 *
 * Deliberately empty: the whole surface is browser-side — a floating dock
 * registered into the frame-wide shell.overlay seat. Nothing host-side needs
 * this package's presence.
 */
/** Host plugin body — the browser surface is registered by the client half. */
export function apply() { }
//# sourceMappingURL=index.js.map